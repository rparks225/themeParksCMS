<!--Open Cont area-->

<div class="container">

        {# $query = 'SELECT * FROM post WHERE ID LIKE '.title(2).'' #}
        {# $block = 'post' #}
        {# $heading = new model() #}
        {# $heading->query($query,$block) #}
        {# $rows = $heading->rows($query) #}

        {-- if ($rows == 0) --}

        {# header('Location: 404') #}

        {-- endif --}

</div>

<!--Close Cont area-->
