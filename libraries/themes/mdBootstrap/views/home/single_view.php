{# $query = 'SELECT * FROM page WHERE Title LIKE \''.strtolower( title() ).'\' ' #}
{# $block = 'single' #}
{# $page = new model() #}
{# $page->query($query, $block) #}
{# $rows = $page->rows($query) #}

{-- if ($rows == 0) --}

          {# tpBlock('404') #}

{-- endif --}