{# $query = 'SELECT * FROM page WHERE Title LIKE \''.title(2).'\' ' #}
{# $block = 'single' #}
{# $page = new model() #}
{# $page->query($query, $block) #}
{# $rows = $page->rows($query) #}

{-- if ($rows == 0) --}

   {# header('Location: 404') #}

{-- endif --}