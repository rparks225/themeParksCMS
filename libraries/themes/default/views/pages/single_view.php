{# $query = 'SELECT * FROM page WHERE Title LIKE \''.title(2).'\' ' #}
{# $block = 'single' #}
{# $page = new model() #}
{# $page->pages($query, $block) #}
{# $rows = $page->rows($query) #}

{-- if ($rows == 0) --}

<!--Open Cont area-->

       {# header('Location: 404') #}

<!--Close Cont area-->

{-- endif --}